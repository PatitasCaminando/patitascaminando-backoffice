import { Component, OnInit, signal } from '@angular/core';import { DatePipe } from '@angular/common';import { FormsModule } from '@angular/forms';import { ApiService } from '../../core/api.service';import { DonationOffer, DonationStatus } from '../../core/models';import { StatusPipe } from '../../shared/status.pipe';
@Component({selector:'app-donations-admin',standalone:true,imports:[FormsModule,StatusPipe,DatePipe],template:`<div class="page-title"><div><span class="eyebrow">Aportes</span><h1>Ofrecimientos de donación</h1></div></div><div class="card table-card"><table><thead><tr><th>Donante</th><th>Artículos</th><th>Fecha</th><th>Estado</th><th></th></tr></thead><tbody>@for(d of rows();track d.id){<tr><td><strong>{{d.firstNames}} {{d.lastNames}}</strong><br><small>{{d.email}} · {{d.phone}}</small></td><td>{{d.selectedItems.join(', ')}}<br><small>{{d.descriptionObservation}}</small></td><td>{{d.submittedAt|date:'dd/MM/yyyy'}}</td><td><span class="status" [attr.data-status]="d.status">{{d.status|statusLabel}}</span></td><td><button class="icon-btn" (click)="selected.set(d);status=d.status;observations=d.internalObservations||''">Gestionar</button></td></tr>}</tbody></table></div>@if(selected()){<div class="modal-backdrop"><div class="modal"><button class="modal-close" (click)="selected.set(null)">×</button><h2>Gestionar donación</h2><label>Estado<select class="form-control" [(ngModel)]="status"><option value="ofrecida">Ofrecida</option><option value="contactada">Contactada</option><option value="entrega_coordinada">Entrega coordinada</option><option value="recibida">Recibida</option><option value="no_aceptada">No aceptada</option><option value="cancelada">Cancelada</option></select></label><label>Observaciones internas<textarea class="form-control" [(ngModel)]="observations"></textarea></label><button class="btn btn-primary btn-block" (click)="save()">Guardar cambio</button></div></div>}`})
export class DonationsAdminComponent implements OnInit{rows=signal<DonationOffer[]>([]);selected=signal<DonationOffer|null>(null);status:DonationStatus='ofrecida';observations='';constructor(private api:ApiService){}ngOnInit(){this.load()}load(){this.api.donations().subscribe(v=>this.rows.set(v))}save(){if(!this.selected())return;this.api.updateDonationStatus(this.selected()!.id,this.status,this.observations).subscribe(()=>{this.selected.set(null);this.load()})}}
