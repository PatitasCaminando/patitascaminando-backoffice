import { Component, signal } from '@angular/core';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { RouterLink } from '@angular/router';import { ApiService } from '../../core/api.service';
@Component({selector:'app-forgot',standalone:true,imports:[ReactiveFormsModule,RouterLink],template:`<div class="auth-page"><div class="auth-card"><img src="assets/branding/logo-color.jpeg" alt="Patitas Caminando"><h1>Recuperar contraseña</h1><p>Te enviaremos instrucciones al correo de tu cuenta interna.</p><form [formGroup]="form" (ngSubmit)="submit()"><label>Correo electrónico<input class="form-control" type="email" formControlName="email"></label>@if(message()){<div class="alert success">{{message()}}</div>}<button class="btn btn-primary btn-block" [disabled]="form.invalid||loading()">Enviar instrucciones</button></form><a routerLink="/login" class="back">← Regresar al inicio de sesión</a></div></div>`})
export class ForgotComponent{loading=signal(false);message=signal('');form=this.fb.nonNullable.group({email:['',[Validators.required,Validators.email]]});constructor(private fb:FormBuilder,private api:ApiService){}submit(){if(this.form.invalid)return;this.loading.set(true);this.api.forgotPassword(this.form.getRawValue().email).subscribe({next:r=>{this.loading.set(false);this.message.set(r.message)},error:()=>{this.loading.set(false);this.message.set('Si la cuenta existe y está activa, recibirás las instrucciones.')}})}}
